// Tipos del módulo runtime stages.js (para el front TS, sin allowJs global).
export const STAGE_LABELS: Record<string, string>
export const STAGE_ORDER: string[]
export function stageLabel(stage: string): string
